//using Assets.Scripts.Views;

namespace Assets
{/*
    using UnityEngine;
    using UnityEditor;

    [CustomEditor(typeof(BoardView))]
    public class GridViewEditor : Editor
    {
        public override void OnInspectorGUI()
        {
            DrawDefaultInspector();

            BoardView gridView = (BoardView)target;
            if (GUILayout.Button("Reset Board"))
            {
                gridView.Initialize();
            }
        }
    }*/
}